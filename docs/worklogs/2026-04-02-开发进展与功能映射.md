# 2026-04-02 开发进展与功能映射

## 1. 当前阶段结论

- 产品方向：`虚拟试穿 + AI 穿搭助手`
- MVP 主链路：`形象创建 -> 服装导入/裁剪 -> 试穿编辑 -> AI 点评 -> 保存与管理`
- 当前前端以 Flutter 本地能力为主，后端负责账号、同步、AI 点评。
- 截至 2026-04-02，P0 中“同步落库”和“设置页同步入口”已落代码；“乱码清理”完成一部分；Android 冒烟已恢复可跑。

## 2. 工程结构

### 2.1 Flutter 客户端

- 目录：`D:\workspace\iFit\fitmirror`
- 入口文件：`lib/main.dart`
- 页面层：`lib/screens`
- 状态层：`lib/providers`
- 模型层：`lib/models`
- 服务层：`lib/services`
- 工具层：`lib/utils`

### 2.2 Spring Boot 后端

- 目录：`D:\workspace\iFit\backend`
- 启动类：`src/main/java/com/fitmirror/FitMirrorApplication.java`
- 分层：`controller / service / repository / entity / dto / config / security`

## 3. PRD 功能与代码对应

### 3.1 已实现

1. 形象管理：拍照/上传/列表管理
   - `fitmirror/lib/screens/avatar/avatar_list_screen.dart`
   - `fitmirror/lib/providers/avatar_provider.dart`

2. 服装截图导入与裁剪
   - `fitmirror/lib/screens/closet/closet_screen.dart`
   - `fitmirror/lib/screens/closet/cloth_crop_screen.dart`

3. 试穿编辑
   - `fitmirror/lib/screens/tryon/tryon_editor_screen.dart`

4. 试穿结果保存
   - `fitmirror/lib/services/image_service.dart`
   - `fitmirror/lib/providers/tryon_provider.dart`
   - `fitmirror/lib/screens/tryon/tryon_result_screen.dart`

5. 用户注册/登录
   - `fitmirror/lib/screens/auth/auth_page.dart`
   - `fitmirror/lib/providers/user_provider.dart`
   - `backend/src/main/java/com/fitmirror/controller/AuthController.java`

6. AI 点评接口链路
   - `fitmirror/lib/services/api_service.dart`
   - `fitmirror/lib/screens/tryon/tryon_result_screen.dart`
   - `backend/src/main/java/com/fitmirror/controller/AiController.java`
   - `backend/src/main/java/com/fitmirror/service/AiService.java`

### 3.2 部分实现

1. 云端同步
   - 前端：
     - `fitmirror/lib/services/sync_service.dart`
     - `fitmirror/lib/screens/profile/profile_screen.dart`
     - `fitmirror/lib/providers/avatar_provider.dart`
     - `fitmirror/lib/providers/cloth_provider.dart`
     - `fitmirror/lib/providers/tryon_provider.dart`
   - 后端：
     - `backend/src/main/java/com/fitmirror/controller/SyncController.java`
     - `backend/src/main/java/com/fitmirror/service/SyncService.java`
   - 当前状态：
     - 已支持上传本地数据
     - 已支持下载云端数据并写回本地 Hive
     - 设置页已接入同步入口
     - 仍需补充真实同步状态展示、冲突策略与完整联调

2. 穿搭历史/评分
   - 试穿历史已具备基础数据
   - 用户自评分体系未完整落地

3. 穿搭组合 Outfit
   - 选择和编辑链路存在
   - 独立 Outfit 模型、列表、管理页未完整落地

### 3.3 未完成或待增强

1. Outfit 完整闭环
2. 用户评分与备注
3. 文件上传链路完整联调
4. 同步后的更多边界处理
5. 全项目中文乱码清理

## 4. 本轮已完成代码改动

### 4.1 同步闭环

- 文件：
  - `fitmirror/lib/services/sync_service.dart`
  - `fitmirror/lib/providers/avatar_provider.dart`
  - `fitmirror/lib/providers/cloth_provider.dart`
  - `fitmirror/lib/providers/tryon_provider.dart`
  - `fitmirror/lib/screens/profile/profile_screen.dart`

- 已完成内容：
  - 下载云端数据并落本地 Hive
  - Provider 增加 `reload()`
  - 设置页同步按钮执行 `上传 -> 下载 -> 刷新列表`
  - 同步结果通过 Snackbar 反馈

### 4.2 AI 点评失败兜底调整

- 文件：
  - `fitmirror/lib/screens/tryon/tryon_result_screen.dart`

- 已完成内容：
  - 不再默认回退 mock 数据
  - 请求失败时直接展示错误提示

### 4.3 文案编码修复

- 文件：
  - `fitmirror/lib/screens/profile/profile_screen.dart`
  - `fitmirror/lib/screens/tryon/tryon_result_screen.dart`

- 已完成内容：
  - 修复字符串编码损坏导致的 Dart 编译错误
  - 修复设置页和试穿结果页主要中文文案
  - 恢复 Android Debug 构建

## 5. 已验证情况

### 5.1 Android 测试环境

- 已安装：
  - Flutter 3.24.0
  - JDK 17
  - Android SDK / emulator / platform-tools / API 34 system image

### 5.2 冒烟结果

- `flutter build apk --debug`：通过
- 模拟器安装运行：通过
- 已验证页面：
  - 首页正常启动
  - 设置页可进入
  - 未登录点击“数据同步”会提示先登录

## 6. 当前遗留问题

1. `flutter analyze` 仍有项目原有问题，不属于本轮修复范围
   - `fitmirror/test/widget_test.dart`
   - `fitmirror/tool/generate_icon.dart`

2. 工作区仍有未提交生成文件
   - `fitmirror/ios/Runner/GeneratedPluginRegistrant.h`
   - `fitmirror/ios/Runner/GeneratedPluginRegistrant.m`

3. 当前分支为：
   - `codex/fix-android-build-strings`

4. 远端 `main` 已有新提交，后续继续开发前需要先同步并决定：
   - 是先合并/提交当前修复分支
   - 还是基于最新 `main` 再开新分支继续开发

## 7. 下次开发建议顺序

1. 先处理当前修复分支的 PR / 合并
2. 同步最新 `main`
3. 继续清理剩余中文乱码
4. 完成 Outfit 最小闭环
5. 补齐同步状态页与真实联调
6. 清理 `flutter analyze` 的现存错误

## 8. 继续开发时的固定规则

1. 先 `git fetch` 和 `git pull --ff-only`
2. 再按 GitFlow 新开分支
3. 提交信息使用规范前缀，例如：
   - `feat: ...`
   - `fix: ...`
   - `refactor: ...`
4. 提交后推送远端并创建 MR / PR
